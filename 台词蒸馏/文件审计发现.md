# 文件审计发现

> 2026-07-07，检查 skill 文件调用路径时的附带发现。

---

## `project/SKILL.md` 是旧版本

- `.claude/skills/denia/SKILL.md`（357 行）— **实际运行的版本**，v0.2.4
- `project/SKILL.md`（339 行）— **v0.2.2 左右的旧快照**，永远不会被加载

### 关键差异

| | `.claude/` 版（运行中） | `project/` 版（GitHub 公开） |
|---|---|---|
| 文件路径 | 绝对路径 | 相对路径 |
| L2 自检锚点 | 角色记忆（"西西听到会说…"） | 旧版（"人说出口的还是论文里摘的"） |
| 记忆触发规则表 | ✅ 有（v0.2.3） | ❌ 无 |
| 禁用句式 | 两层（AI论证腔 + 通用） | 一层 |
| 事实表述 | "残星会制造" | "残星会改造" |

### 影响

GitHub 公开仓库推送的是 `project/` 目录。当前 `personal` 和 `personality-distill` 分支的 `project/SKILL.md` 都是这个旧版本。**外部用户如果 clone 后直接使用 `project/SKILL.md`，拿到的是过时的 skill。**

### 建议（日后处理）

- 将 `.claude/skills/denia/SKILL.md` 同步到 `project/SKILL.md`（或让 `project/SKILL.md` 指向它）
- 或者从 `project/` 中删除 `SKILL.md`，在 README 中说明 skill 入口在 `.claude/` 下
