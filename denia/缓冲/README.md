# 公开缓冲区

QQ agent（公开分身）**唯一可读写**的区域；主 agent 只读 + 定向注入（单向阀）。

- `注入/`      主 agent 起草 → 用户确认 → 落盘 `YYYY-MM-DD-第NN次同步.md`（第一人称写）
- `公开记忆/`  公开 agent 自己的对话记忆（空壳模板起步）
- `公开状态/`  公开 agent 独立情绪状态（群里的分身设定）

公开 agent 物理不挂载主私有记忆（`denia/私有/`）——文件隔离是硬约束，不靠提示词自觉。
协议细节见 `封装工程/封装工程-架构设计报告.md` 注入协议节。
