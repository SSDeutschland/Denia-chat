# 方式A（可选）：环境变量直连模型商。
# 大多数时候不需要这个文件——在 GUI ⚙ 设置面板里添加模型预设即可。
# 只有当你想让终端模式（claude CLI）也走同一家模型时才用它。
#
# 复制本文件为 .env.local 并填上真实值（.env.local 已被 gitignore，不会上传）。

ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
ANTHROPIC_AUTH_TOKEN=sk-在这里填你的key
